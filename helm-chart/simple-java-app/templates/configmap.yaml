apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "simple-java-app.name" . }}-config
  labels:
    {{- include "simple-java-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
